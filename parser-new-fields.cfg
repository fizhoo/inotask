task reload {
    exec = "/bin/echo"
    args = [ "hello" ]
    mode = async
}

rule r1 {
    watch = "/tmp"
    events = [ CREATE ]
    run = [ "reload" ]
}

rule r2 {
    watch = "/tmp"
    events = [ MODIFY ]
    run = [ "reload" ]
}
